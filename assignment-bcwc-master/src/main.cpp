#include "../header/RShell.hpp"

#include <string>
#include <iostream>
using namespace std;

int main()
{
RShell r;
r.run();

	// vector<Token> b;
	// b.push_back(Token("cd"));
	// b.push_back(Token(".."));


	// vector<Token> c;
	// c.push_back(Token("ls"));
	// c.push_back(Token("-a"));

	// Component* a = new Command(b);
	// Component* d = new Command(c);

	// Component* e = new And(a, d);

	// e->execute();

    return 0;
}
